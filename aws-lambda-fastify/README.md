fastify/aws-lambda-fastify: https://github.com/fastify/aws-lambda-fastify

#### npm package

- install

  - yarn workspace `workspace-name` add (--dev) `package-name` --exact

- upgrade
  - yarn outdated
  - yarn upgrade-interactive --latest
